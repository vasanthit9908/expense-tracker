import { EmptyState } from "@/components/page-header";

export default function NotFound() {
  return <EmptyState title="Page not found" hint="Use the sidebar to go back to Dashboard or a list view." />;
}
